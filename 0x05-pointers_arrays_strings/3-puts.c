#include "main.h"

/**
 * function that prints a string by new line
 *
 *@s: prints a string
 *
 * Return: nothing
 *
*/
void _puts(char *str);
{
	int i =  0;
	while (s[i])
		i++;
	
	while (i--)
	{
		_putchar (s[i]);
	}
	_putchar ('\n');
}
