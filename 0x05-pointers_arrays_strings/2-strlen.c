#include "main.h"

/**
 * function that returns the length of a string
 *
 *@s: string
 *
 * Return: lenght
*/
int _strlen(char *s);
{
	int len = 0;

	while (strlen != '\0')
	{
		len++;
	}

	return (len);
}
